@model Atlas_Web.Pages.Profile.IndexModel
[
    @foreach (var item in @Model.BarDataSet)
    {
        @:{"key": "@item.Key", "count": "@item.Count", "percent": "@item.Percent", "href": "@item.Href", "title_one": "@item.TitleOne", "title_two": "@item.TitleTwo"
        @if(!string.IsNullOrEmpty(item.DateTitle)){<text>, "date_title":"@item.DateTitle"</text>}
        @if(!string.IsNullOrEmpty(item.Date)){<text>, "date":"@item.Date"</text>}
        @:}
        @if(item != @Model.BarDataSet.Last()){<text>,</text>}
    }
]
